{
  "name": "peach-blossom-spring",
  "version": "0.1.0",
  "private": true,
  "description": "桃花源：可思考的生命模擬器派遣 for GitHub Pages.",
  "scripts": {
    "build": "npm --prefix webview-ui run build",
    "test": "npm --prefix webview-ui run test"
  }
}
